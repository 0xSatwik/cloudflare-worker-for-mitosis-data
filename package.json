{
  "name": "mito-api",
  "version": "1.0.0",
  "description": "API for MITO token holders data",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "build": "mkdir -p dist && cp -r src package.json wrangler.toml dist/",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "itty-router": "^4.0.22"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}